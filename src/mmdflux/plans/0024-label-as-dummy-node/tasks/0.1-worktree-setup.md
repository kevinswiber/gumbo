# Task 0.1: Create Git Worktree

## Objective
Set up an isolated git worktree off `main` for the label-as-dummy-node experiment.

## Steps

```bash
cd /Users/kevin/src
git -C mmdflux worktree add ../mmdflux-label-dummy -b label-dummy-experiment main
```

This creates:
- A new worktree at `/Users/kevin/src/mmdflux-label-dummy`
- A new branch `label-dummy-experiment` based on `main`

## Verification

```bash
cd /Users/kevin/src/mmdflux-label-dummy
git branch --show-current  # should show: label-dummy-experiment
cargo test                 # should pass — same as main
```

## Acceptance Criteria
- [ ] Worktree exists at `/Users/kevin/src/mmdflux-label-dummy`
- [ ] Branch is `label-dummy-experiment`
- [ ] All tests pass in the worktree
